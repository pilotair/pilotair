{
  "name": "pilotair",
  "type": "module",
  "devDependencies": {
    "@types/bun": "latest",
    "@types/react-dom": "^18.2.24"
  },
  "scripts": {
    "dev": "bun --bun src/main.ts ",
    "dev_front": "bun run --cwd front-app dev",
    "build": "bun run --cwd front-app build",
    "publish": "bun run build && docker build -t pilotair:latest ."
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  },
  "dependencies": {
    "antd": "^5.16.1",
    "mime": "^4.0.1",
    "react": "next",
    "react-dom": "next"
  }
}
